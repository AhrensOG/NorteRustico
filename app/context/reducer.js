export const reducer = (state, action) => {
  switch (action.type) {
    // case "Example":
    //   return {
    //     ...state,
    //     example: action.payload
    //   };
    default:
      return {
        ...state,
      };
  }
};
